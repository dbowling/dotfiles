#!/bin/sh

set -e # -e: exit on error
# asdf is not in use
# see Volta for node management

# an example of node, which I'm currently running via Volta
# if [ ! -f "$HOME/.oh-my-zsh/oh-my-zsh.sh" ]; then
    # echo "Add nodejs plugin"
    # /bin/sh -c "$(asdf plugin add nodejs https://github.com/asdf-vm/asdf-nodejs.git)"
    # echo "Install LTS"
    # /bin/sh -c "$(asdf install nodejs lts)"
    # echo "Configure LTS as global default"
    #  /bin/sh -c "$(asdf global nodejs lts)"
    
# fi